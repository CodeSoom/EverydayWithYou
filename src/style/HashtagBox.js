import styled from '@emotion/styled';

const HashtagBox = styled.div({
  textAlign: 'left',
  margin: '8px 0',
  '& input': {
    border: 'none',
    padding: '8px',
    margin: '4px',
    borderRadius: '4px',
  },
});

export default HashtagBox;
